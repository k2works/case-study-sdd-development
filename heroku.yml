build:
  docker:
    web: Dockerfile.heroku
run:
  web: sh -c 'FRONTEND_PORT=${PORT:-3001}; BACKEND_PORT=${INTERNAL_API_PORT:-3000}; PORT=$BACKEND_PORT node apps/backend/dist/main.js & exec npx next start apps/frontend --hostname 0.0.0.0 --port $FRONTEND_PORT'
