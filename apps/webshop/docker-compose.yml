services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: fleur_memoire
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
